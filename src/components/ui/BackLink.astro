---
import { ArrowLeft } from "lucide-react";

interface Props {
  href: string;
  label: string;
}

const { href, label } = Astro.props;
---

<a href={href} class="inline-flex items-center gap-1.5 text-sm text-primary hover:underline font-sans">
  <ArrowLeft className="w-4 h-4" />
  {label}
</a>
