---
import SanityImage from "@/components/common/SanityImage";
import SocialLinks from "@/components/common/SocialLinks";
import { INSTITUTE_NAME, MAESTRO_NAME } from "@/lib/constants";
import type { QuerySocialLink, SanityImageBase } from "@/lib/sanity-derived-types";

interface Props {
  logo?: SanityImageBase | null;
  description?: string | null;
  socialLinks: QuerySocialLink[];
}

const { logo, description, socialLinks } = Astro.props;
---

<div>
  <a href="/" class="flex items-center gap-3 mb-4 w-fit">
    {
      logo && (
        <SanityImage
          client:visible
          image={logo}
          width={56}
          height={56}
          className="w-14 h-14 rounded-full object-cover"
        />
      )
    }
    <div>
      <p class="text-[13px] font-semibold text-secondary uppercase font-sans tracking-wide">{INSTITUTE_NAME}</p>
      <p class="text-xl font-bold text-white font-serif">{MAESTRO_NAME}</p>
    </div>
  </a>

  {
    description && (
      <p class="text-sm text-primary-foreground/80 leading-relaxed font-sans max-w-sm mb-6">{description}</p>
    )
  }

  <SocialLinks
    client:visible
    socialLinks={socialLinks}
    className="gap-3"
    iconClassName="w-5 h-5 text-white"
    linkClassName="size-10"
  />
</div>
