---
import type { FlatMenuItem } from "@/lib/utils/link-utils";

interface Props {
  label: string;
  items: FlatMenuItem[];
}

const { label, items } = Astro.props;
---

<nav aria-labelledby="footer-nav-label">
  <h3 id="footer-nav-label" class="heading-label mb-4">{label}</h3>
  <ul class="space-y-2">
    {
      items.map((link) => (
        <li>
          <a
            href={link.href}
            class="caption-text-primary-foreground hover:text-secondary transition-colors"
          >
            {link.label}
          </a>
        </li>
      ))
    }
  </ul>
</nav>
